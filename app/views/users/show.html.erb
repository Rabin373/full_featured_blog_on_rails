<h1 align="center", style="font-family:UnifrakturMaguntia;">welcome to <%= @user.username %></h1>

<div class="row">
<div class="col-xs-md-4 col-md-offset-4 center">
<%= gravitar_for @user %>
</div>
</div>

<h4 align="center"><%= @user.username %>'s articles</h4>

<%= render 'articles/article', obj: @user.articles %>